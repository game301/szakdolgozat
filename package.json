{
    "name": "mushoku-cult",
    "private": true,
    "version": "0.0.0",
    "type": "module",
    "scripts": {
        "dev": "concurrently \"cd backend && npm run dev\" \"cd frontend && npm run dev\" "
    },
    "devDependencies": {
        "concurrently": "^8.2.2"
    }
}
